--- a/sabri-unified-application-shell.php
+++ b/sabri-unified-application-shell.php
@@
- * Version: 1.0.0
+ * Version: 1.0.1
@@
-define( 'SABRI_SHELL_VERSION', '1.0.0' );
+define( 'SABRI_SHELL_VERSION', '1.0.1' );

--- a/includes/class-renderer.php
+++ b/includes/class-renderer.php
@@
 	private static function can_show_create( array $settings ) {
-		if ( ! is_user_logged_in() || ! current_user_can( 'edit_posts' ) ) {
+		if ( ! is_user_logged_in() || SafeMode::disabled() ) {
 			return false;
 		}
 
-		$user  = wp_get_current_user();
-		$roles = is_array( $user->roles ) ? $user->roles : array();
+		$user         = wp_get_current_user();
+		$roles        = is_array( $user->roles ) ? $user->roles : array();
+		$role_allowed = current_user_can( 'edit_posts' )
+			&& (bool) array_intersect( $roles, $settings['header']['allowed_roles'] );
 
-		return (bool) array_intersect( $roles, $settings['header']['allowed_roles'] );
+		/**
+		 * Filters final Create visibility after login and Safe Mode checks.
+		 *
+		 * Integrations such as File 22 may replace the legacy role-list result
+		 * with a stricter central permission and adapter-availability decision.
+		 * Safe Mode cannot be overridden because it returns before this filter.
+		 *
+		 * @param bool                $role_allowed Legacy shell result.
+		 * @param int                 $user_id      Current user ID.
+		 * @param array<string,mixed> $settings     Current shell settings.
+		 */
+		return (bool) apply_filters(
+			'sabri_shell_can_show_create',
+			$role_allowed,
+			(int) $user->ID,
+			$settings
+		);
 	}
