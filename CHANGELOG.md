# Changelog

## 1.0.1 — Proposed

- Add the official `sabri_shell_can_show_create` integration contract for File 22.
- Preserve login and Safe Mode as non-overridable shell safeguards.
- Preserve the legacy Administrator/Editor role and `edit_posts` result as the fallback when no trusted integration is active.
- Pass current user ID and shell settings to the integration filter.

## 1.0.0

- Existing audited Unified Application Shell package.
